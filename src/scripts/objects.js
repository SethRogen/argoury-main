importPackage(com.ziotic)
importPackage(com.ziotic.engine.tick)
importPackage(com.ziotic.logic.map)

function handleObjectOption1(player, objId, obj) {
    switch(objId) {
    default:
        logger().debug("Unhandled object option 1 [objId=" + objId + "]")
        break
    }
}

function handleObjectOption2(player, objId, obj) {
    switch(objId) {
    default:
        logger().debug("Unhandled object option 2 [objId=" + objId + "]")
        break
    }
}